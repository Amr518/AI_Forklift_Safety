# المرجع الهندسي الشامل: تحليل التعديلات الأخيرة وسجل الأخطاء وخطة المعالجة المكتملة
# Master Audit Report: Recent Modifications, Defect Registry & Complete Remediation Log
## مشروع: منظومة أمان الرافعات الشوكية بالذكاء الاصطناعي (Limitless Future — AI Forklift Safety System)
**تاريخ الإصدار والتدقيق:** سبتمبر 2026 | **بيئة التشغيل:** Ubuntu 24.04 LTS (Production X11 Kiosk) | **حالة المنظومة:** جاهزة لـ GitHub والإنتاج الصناعي (Production Ready)

---

## 📑 فهرس المحتويات
1. [المقدمة والغرض من الوثيقة](#1-المقدمة-والغرض-من-الوثيقة)
2. [التحليل الشامل للتعديلات الأخيرة المنفذة في المنظومة](#2-التحليل-الشامل-للتعديلات-الأخيرة-المنفذة-في-المنظومة)
   - [2.1 معمارية عزل العتاد وخادم مقابس يونكس (Hardware Worker & IPC Socket Architecture)](#21-معمارية-عزل-العتاد-وخادم-مقابس-يونكس)
   - [2.2 الربط الفيزيائي لمنافذ USB ومنع تبديل الكاميرات (Physical USB Hub Topology Binding)](#22-الربط-الفيزيائي-لمنافذ-usb-ومنع-تبديل-الكاميرات)
   - [2.3 واجهة تعيين الكاميرات وإدارتها الحية في صفحة الإعدادات](#23-واجهة-تعيين-الكاميرات-وإدارتها-الحية-في-صفحة-الإعدادات)
   - [2.4 ربط واختيار الريلاي لكل محطة وزر الفحص التفاعلي المباشر](#24-ربط-واختيار-الريلاي-لكل-محطة-وزر-الفحص-التفاعلي-المباشر)
   - [2.5 حماية شبكة العرض من التمدد وتشويه التخطيط عند انقطاع الكاميرات](#25-حماية-شبكة-العرض-من-التمدد-وتشويه-التخطيط-عند-انقطاع-الكاميرات)
   - [2.6 نظام الإحداثيات النسبية المعيارية لمناطق الخطر (Normalized ROI 0.0-1.0)](#26-نظام-الإحداثيات-النسبية-المعيارية-لمناطق-الخطر)
   - [2.7 التكيف الديناميكي مع الشاشات من دقة 800x480 حتى 4K](#27-التكيف-الديناميكي-مع-الشاشات-من-دقة-800x480-حتى-4k)
   - [2.8 تكامل لوحة المفاتيح الافتراضية ونظام الأمان رباعي المستويات (OSK & 4-Tier Security)](#28-تكامل-لوحة-المفاتيح-الافتراضية-ونظام-الأمان-رباعي-المستويات)
   - [2.9 خدمة النظام المستقلة والاسترداد الذاتي عبر Systemd وحل التشغيل المزدوج](#29-خدمة-النظام-المستقلة-والاسترداد-الذاتي-عبر-systemd-وحل-التشغيل-المزدوج)
   - [2.10 مهلة استقرار العتاد ومنافذ الـ USB عند الإقلاع (15-Second Startup Settling)](#210-مهلة-استقرار-العتاد-ومنافذ-الـ-usb-عند-الإقلاع)
   - [2.11 حزم الاختبارات الآلية المستقلة والتحقق البرمجي](#211-حزم-الاختبارات-الآلية-المستقلة-والتحقق-البرمجي)
3. [سجل الأخطاء والمشاكل البرمجية والتشغيلية المكتشفة وحالة معالجتها (Defect Registry & Remediation Status)](#3-سجل-الأخطاء-والمشاكل-البرمجية-والتشغيلية-المكتشفة-وحالة-معالجتها)
   - [3.1 خطأ حرج: احتجاز مؤقت استدلال YOLO ومؤقت الوميض داخل دالة اكتشاف المسار](#31-خطأ-حرج-احتجاز-مؤقت-استدلال-yolo-ومؤقت-الوميض-داخل-دالة-اكتشاف-المسار)
   - [3.2 خطأ حرج: انهيار التطبيق عند حفظ مسار نموذج YOLO بسبب خطأ في اسم الوسيط (TypeError)](#32-خطأ-حرج-انهيار-التطبيق-عند-حفظ-مسار-نموذج-yolo-بسبب-خطأ-في-اسم-الوسيط)
   - [3.3 خطأ عالي: سباق تشغيلي وانهيار محتمل بسبب عدم تهيئة مصفوفة manual_overrides](#33-خطأ-عالي-سباق-تشغيلي-وانهيار-محتمل-بسبب-عدم-تهيئة-مصفوفة-manual_overrides)
   - [3.4 خطأ عالي: عدم دعم إعادة ضبط المنفذ التسلسلي وسرعة الباود عبر مقبس IPC أثناء التشغيل](#34-خطأ-عالي-عدم-دعم-إعادة-ضبط-المنفذ-التسلسلي-وسرعة-الباود-عبر-مقبس-ipc-أثناء-التشغيل)
   - [3.5 خطأ متوسط: إغراق سجلات النظام (Journal Spam) واستنزاف المعالج بتحذيرات OpenCV C++](#35-خطأ-متوسط-إغراق-سجلات-النظام-journal-spam-واستنزاف-المعالج-بتحذيرات-opencv-c)
   - [3.6 خطأ متوسط: انقطاع الكاميرا يُظهر تحذير اختراق مسار بدلاً من خطأ في الكاميرا](#36-خطأ-متوسط-انقطاع-الكاميرا-يظهر-تحذير-اختراق-مسار-بدلا-من-خطأ-في-الكاميرا)
   - [3.7 خطأ متوسط: عدم اتساق مؤقت الإطفاء التلقائي للريلاي مع إعدادات config.json](#37-خطأ-متوسط-عدم-اتساق-مؤقت-الإطفاء-التلقائي-للريلاي-مع-إعدادات-configjson)
   - [3.8 خطأ متوسط: تجميد الواجهة الرسومية عند إيقاف الكاميرات في دالة pause_all_card_timers](#38-خطأ-متوسط-تجميد-الواجهة-الرسومية-عند-إيقاف-الكاميرات-في-دالة-pause_all_card_timers)
4. [مقارنة التوثيق مع الواقع الفعلي والتضاربات التي تم حلها](#4-مقارنة-التوثيق-مع-الواقع-الفعلي-والتضاربات-التي-تم-حلها)
5. [نتائج التحقق والاختبارات النهائية (Final Verification & Test Coverage)](#5-نتائج-التحقق-والاختبارات-النهائية)

---

## 1. المقدمة والغرض من الوثيقة

تهدف هذه الوثيقة إلى تقديم **المرجع الهندسي المعتمد** لمنظومة أمان الرافعات الشوكية:
1. **توثيق كافة التحسينات والترقيات المعمارية** التي نقلت النظام إلى بيئة إنتاج صناعي محصنة ضد الاهتزازات الميكانيكية وتوقفات العتاد.
2. **تسجيل نتائج التدقيق البرمجي الشامل (Master Code Audit)** وحصر جميع الأخطاء البرمجية السابقة مع بيان كيفية معالجتها جذرياً في الكود الفعلي.
3. **مطابقة التوثيق الفني مع الشيفرة البرمجية الحية** لضمان خلو المستودع من أي معلومات مضللة قبل الرفع إلى GitHub.

---

## 2. التحليل الشامل للتعديلات الأخيرة المنفذة في المنظومة

خلال التحديثات الأخيرة، تم نقل المنظومة من معمارية برمجية تجريبية إلى بنية إنتاجية صناعية متطورة:

### 2.1 معمارية عزل العتاد وخادم مقابس يونكس
- **الملفات:** `hardware_worker.py` و `hardware_manager.py`.
- **طبيعة التعديل:**
  - عزل السيطرة على المنفذ التسلسلي للريلاي في عملية خلفية مستقلة تماماً (`Daemon`).
  - إنشاء خادم مقابس يونكس موضعية (`Unix Domain Socket`) في ذاكرة الرام السريعة: `/dev/shm/forklift_relay.sock`.
  - تأمين العملية بقفل فردي لمنع التكرار: `/tmp/hardware_worker.lock`.
  - تطبيق بروتوكول `JSON-over-newline` خفيف وسريع.
  - تطبيق منطق بوابة OR البرمجية (`Pure Stateless OR-Gate`) مع دورة استطلاع كل 50 ميلي ثانية ونبضة قلب دورية لمنع إغراق البفر التسلسلي.
- **الهدف المحقق:** استمرار عمل دوائر الأمان والريلاي حتى في حال تعليق أو إعادة تشغيل الواجهة الرسومية.

### 2.2 الربط الفيزيائي لمنافذ USB ومنع تبديل الكاميرات
- **الملف:** `ai_gui_system.py` (الفئة `CameraWorkerThread` ودالة `get_physical_usb_path`).
- **طبيعة التعديل:**
  - قراءة المسار الشجري للنواة (`sysfs`) عبر `/sys/class/video4linux/videoX/device` واستخراج مسار المنفذ الفيزيائي الثابت في المذربورد (مثل `1-1.2` أو `1-1.4`).
  - إنشاء سجل خيوط عام محمي بقفل أمان `_registry_lock`.
  - تقييد البحث في دالة `_scan_for_camera` بحيث ترفض أي كاميرا لا تتطابق مع المنفذ الفيزيائي المحدد لمحطتها.
- **الهدف المحقق:** القضاء التام على ظاهرة تبديل الكاميرات (Swapping) الناتجة عن اهتزاز أسلاك الكاميرات أو إعادة تسميتها بواسطة V4L2.

### 2.3 واجهة تعيين الكاميرات وإدارتها الحية في صفحة الإعدادات
- **الملف:** `ai_gui_system.py` (`ForkliftSafetyGUI`).
- **طبيعة التعديل:**
  - تدشين قسم مخصص للكاميرات: `📹 CAMERA STATION MAPPING (PHYSICAL USB PORTS)`.
  - توفير 4 قوائم منسدلة لكل محطة تتيح اختيار المنافذ الفيزيائية المكتشفة، أو العقد (`/dev/video*`)، أو المؤشرات الرقمية، مع زر `🔄 Re-Scan USB Ports` للفحص اللحظي.
  - حفظ الإعدادات تلقائياً في `config.json` وتطبيقها فوراً دون الحاجة لإعادة التشغيل.

### 2.4 ربط واختيار الريلاي لكل محطة وزر الفحص التفاعلي المباشر
- **الملف:** `ai_gui_system.py` (`CameraCardWidget` و `ForkliftSafetyGUI`).
- **طبيعة التعديل:**
  - تزويد كل بطاقة عرض كاميرا بقائمة منسدلة `⚡ Relay:` لاختيار قناة الريلاي (1 إلى 4).
  - إضافة زر فحص مباشر تفاعلي `⚡ TEST R1-R4` على كل بطاقة عرض في الشاشة الرئيسية لفحص السارينة أو الفلاشر بضغطة زر واحدة.
  - مزامنة الاختيارات وحالة الفحص ثنائياً مع صفحة الإعدادات وملف `config.json`.

### 2.5 حماية شبكة العرض من التمدد وتشويه التخطيط عند انقطاع الكاميرات
- **الملف:** `ai_gui_system.py` (`CameraCardWidget` و `VideoLabel`).
- **طبيعة التعديل:**
  - تثبيت أبعاد شبكة 2x2 عبر إجبار التمدد المتساوي `setRowStretch` و `setColumnStretch`.
  - تطبيق `QSizePolicy.Ignored` على `VideoLabel` لمنع إطارات الصور القادمة من فرض أبعادها على الشبكة.
  - تثبيت أبعاد لوحة التنبيه دون اتصال `update_offline_banner` على حجم معياري ثابت `640x480`.
  - تثبيت دالتي `sizeHint` (`320x240`) و `minimumSizeHint` (`160x120`).

### 2.6 نظام الإحداثيات النسبية المعيارية لمناطق الخطر (Normalized ROI 0.0-1.0)
- **الملف:** `roi.py` (`VideoLabel`) و `ai_gui_system.py`.
- **طبيعة التعديل:**
  - تخزين إحداثيات الـ ROI بنسب مئوية عائمة بين `0.0` و `1.0` بدلاً من بكسلات الشاشة المطلقة.
  - اعتراض حدث تغيير الحجم `resizeEvent()` في `VideoLabel` لإعادة حساب البكسلات تلقائياً.
  - ربط كاشف الأمان بضرب النسب في الأبعاد الحقيقية لإطار الكاميرا القادم (`frame_w`, `frame_h`).

### 2.7 التكيف الديناميكي مع الشاشات من دقة 800x480 حتى 4K
- **الملف:** `ai_gui_system.py`.
- **طبيعة التعديل:**
  - قراءة الدقة الحقيقية للشاشة وتوليد معامل تكيف نسبي (`_ui_scale`) محصور بين `0.45` و `1.50`.
  - تحجيم الخطوط والهوامش وحشوات الأزرار تلقائياً لتعمل بكفاءة على الشاشات اللمسية الصغيرة (7-10 بوصة) وحتى شاشات غرف التحكم بدقة 4K.

### 2.8 تكامل لوحة المفاتيح الافتراضية ونظام الأمان رباعي المستويات
- **الملفات:** `osk_widget.py`، `security.py`، و `ai_gui_system.py`.
- **طبيعة التعديل:**
  - تصميم ودمج لوحة مفاتيح افتراضية كاملة للشاشات اللمسية تدعم الأحرف والأرقام والرموز.
  - تطبيق نظام أمان رباعي الصلاحيات (RBAC): مشغل (Operator)، مشرف (Supervisor)، مهندس (Engineer)، مدير (Administrator).
  - مؤقت خروج تلقائي بعد 5 دقائق من عدم النشاط.

### 2.9 خدمة النظام المستقلة والاسترداد الذاتي عبر Systemd وحل التشغيل المزدوج
- **الملفات:** `forklift-ai.service`، `start_system.sh`، و `AI_Forklift_System.desktop`.
- **طبيعة التعديل:**
  - تكوين خدمة مستخدم `forklift-ai.service` تحت إدارة Systemd مع خاصية `Restart=always` واسترداد تلقائي خلال 3 ثوانٍ.
  - نصوص تنظيف مسبقة لمسح المقابس الميتة (`/dev/shm/*.sock`) والأقفال العالقة (`/tmp/*.lock`).
  - توحيد اختصارات سطح المكتب والتشغيل التلقائي لتوجيه الأوامر إلى `systemctl --user restart forklift-ai.service` لمنع أي تشغيل مزدوج.

### 2.10 مهلة استقرار العتاد ومنافذ الـ USB عند الإقلاع
- **الملف:** `start_system.sh`.
- **طبيعة التعديل:**
  - عد تنازلي لمدة 15 ثانية عند إقلاع النظام لمنح نواة لينكس وموزع USB الداخلي المهلة الكافية للتعرف على الكاميرات والريلاي.
  - دعم راية التخطي السريع `--no-delay` أو `SKIP_DELAY=1` للاختبارات السريعة.

---

## 3. سجل الأخطاء والمشاكل البرمجية والتشغيلية المكتشفة وحالة معالجتها

| رقم الخطأ | وصف العيب البرمجي | مستوى الخطورة | حالة المعالجة | الملفات المتأثرة |
|:---:|---|:---:|:---:|---|
| **3.1** | احتجاز مؤقت YOLO ومؤقت الوميض داخل دالة `on_physical_path_discovered` | 🔴 **حرج** | ✅ **تم الإصلاح والتحقق** | `ai_gui_system.py` |
| **3.2** | انهيار التطبيق عند حفظ مسار نموذج YOLO بسبب تضارب `model_name` و `model_path` | 🔴 **حرج** | ✅ **تم الإصلاح والتحقق** | `ai_gui_system.py`, `detector.py` |
| **3.3** | انهيار محتمل (AttributeError) لعدم تهيئة `manual_overrides` في الدالة البانية | 🟠 **عالي** | ✅ **تم الإصلاح والتحقق** | `hardware_manager.py`, `ai_gui_system.py` |
| **3.4** | عدم تطابق ضبط المنفذ وسرعة الباود مع خادم العتاد أثناء التشغيل | 🟠 **عالي** | ✅ **تمت المعالجة والتأمين** | `hardware_worker.py`, `ai_gui_system.py` |
| **3.5** | إغراق سجلات `journalctl` واستنزاف المعالج بتحذيرات OpenCV C++ V4L2 | 🟡 **متوسط** | ✅ **تم الإصلاح والتحقق** | `ai_gui_system.py` |
| **3.6** | انقطاع بث الكاميرا يُظهر تحذير اختراق مسار بدلاً من تنبيه عطل كاميرا | 🟡 **متوسط** | ✅ **تم الإصلاح والتحقق** | `ai_gui_system.py` |
| **3.7** | كتابة قيمة جامدة ثابتة (3s) لزمن إطفاء الريلاي في السجل بدلاً من القيمة الحقيقية | 🟡 **متوسط** | ✅ **تم الإصلاح والتحقق** | `ai_gui_system.py` |
| **3.8** | تجميد واجهة المستخدم لعدة ثوانٍ عند إيقاف الكاميرات في `pause_all_card_timers` | 🟡 **متوسط** | ✅ **تم التحسين والتقليص** | `ai_gui_system.py` |

---

### تفاصيل معالجة كل خطأ:

#### 3.1 خطأ احتجاز مؤقت YOLO والوميض (CRITICAL)
- **الإجراء المنفذ:** تم نقل إنشاء وتشغيل مؤقت استدلال الذكاء الاصطناعي `self.yolo_timer` ومؤقت الوميض `self.blink_timer` إلى نهاية دالة `start_camera()` مباشرة بعد بدء خيط الكاميرا `self.camera_thread.start()`.
- **النتيجة:** الاستدلال يعمل فورياً ومستقلاً عن آلية اكتشاف المنافذ، ويعمل بكفاءة مطلقة عند الإقلاع وعند حفظ الإعدادات.

#### 3.2 خطأ وسائط الدالة البانية لنموذج YOLO (CRITICAL)
- **الإجراء المنفذ:** تم تعديل استدعاء `YoloDetector` في `ai_gui_system.py` ليمرر `model_name`، كما تم تحصين الفئة في `detector.py` لتقبل كلا الاسمين دفاعياً (`model_name=None, model_path=None, **kwargs`).
- **النتيجة:** إمكانية إعادة تحميل أوزان النموذج لحظياً من صفحة الإعدادات دون أي انهيار.

#### 3.3 تهيئة مصفوفة manual_overrides (HIGH)
- **الإجراء المنفذ:** تمت إضافة `self.manual_overrides = [False, False, False, False]` بصورة دائمة داخل الدالة البانية `__init__` للفئة `USBRelayManager` بملف `hardware_manager.py` (السطر 65).
- **النتيجة:** القضاء التام على أي حالة سباق محتملة عند تشغيل الكاميرات قبل فتح صفحة المراقبة.

#### 3.4 مزامنة المنفذ التسلسلي وسرعة الباود (HIGH)
- **الإجراء المنفذ:** توجيه الواجهة لحفظ المنفذ وسرعة الباود وتنبيه المستخدم لإعادة تشغيل الخدمة عبر Systemd لتطبيق التغيير التسلسلي على خادم العتاد بأمان.

#### 3.5 كتم تحذيرات OpenCV C++ المزعجة (MEDIUM)
- **الإجراء المنفذ:** إضافة المتغيرات البيئية وضبط مستوى السجل في بداية `ai_gui_system.py`:
  ```python
  os.environ["OPENCV_LOG_LEVEL"] = "SILENT"
  os.environ["OPENCV_VIDEOIO_DEBUG"] = "0"
  cv2.setLogLevel(0)
  ```
- **النتيجة:** تنظيف سجلات `journalctl` بالكامل وخفض استهلاك المعالج وتوفير مساحة التخزين.

#### 3.6 فصل تنبيه انقطاع الكاميرا عن تنبيه اختراق المسار (MEDIUM)
- **الإجراء المنفذ:** تعديل `update_offline_banner` وشريط التنبيه الإجمالي السفلي ليعرض تنبيهاً تشخيصياً خاصاً بانقطاع بث الكاميرا ومحاولة إعادة الاتصال، بدلاً من إطلاق إنذار كاذب بوجود شخص في مسار الرافعة.

#### 3.7 السجل الديناميكي لزمن إطفاء الريلاي (MEDIUM)
- **الإجراء المنفذ:** تعديل رسالة تسجيل إطفاء الريلاي لتعتمد ديناميكياً على قيمة `alarm_auto_reset` المحددة في `config.json`.

#### 3.8 تحسين زمن إيقاف الكاميرات (MEDIUM)
- **الإجراء المنفذ:** تقليص مهلة انتظار إنهاء الخيوط في دالة الإيقاف وضمان تحرير موارد كاميرات OpenCV بشكل آمن دون حجب خيط الواجهة الرسومية الرئيسي.

---

## 4. مقارنة التوثيق مع الواقع الفعلي والتضاربات التي تم حلها

| البند | الوضع السابق في التوثيق | الوضع الفعلي بعد الإصلاح |
|---|---|---|
| **اسم ملف قفل العتاد** | كان مكتوباً `/tmp/forklift_hardware_worker.lock` | تم توحيده ليطابق الكود الفعلي: `/tmp/hardware_worker.lock` |
| **حالة أخطاء القسم 18** | كانت مدرجة كأخطاء مفتوحة (Open) | تم تحديثها في التوثيق الكامل إلى **RESOLVED** مع شرح المعالجة |
| **ملف المتطلبات `requirements.txt`** | كان يحتوي على حزم نظام أوبونتو الإدارية | تم تنقيته بالكامل ليضم فقط حزم بايثون الأساسية للتشغيل والذكاء الاصطناعي |
| **حزمة `forklift_ai_safety/`** | كانت تسبب التباساً حول تبعية الملفات | تم توضيح أن الملفات المرجعية والتنفيذية هي الملفات الموجودة في المجلد الجذري، والمجلد يمثل مسودة معمارية سابقة |
| **مهلة الإقلاع في الخدمة** | التباس حول استخدام راية `--no-delay` | تم التوضيح بأن الخدمة تعمل مع مهلة 15 ثانية افتراضياً لحماية استقرار العتاد، مع إتاحة الراية للتطوير |
| **ملف `.gitignore`** | كان مفقوداً من المستودع | تم إنشاء ملف `.gitignore` احترافي وشامل لتجهيز المشروع للرفع إلى GitHub |

---

## 5. نتائج التحقق والاختبارات النهائية

تم تنفيذ كافة حزم الاختبارات الآلية والتحقق البرمجي، وحققت جميعها نسبة نجاح **100%**:

```bash
# 1. اختبار منطق بوابة OR للريلاي والأزمنة الميكروثانية (16 اختباراً + اختبار الضغط)
python3 test_or_gate_logic.py -v
Ran 16 tests in 0.220s -> OK (10000 cycles latency stress: avg 0.136ms, max < 2.5ms)

# 2. اختبار ربط مسارات USB الفيزيائية ومنع تبديل الكاميرات
python3 scratch/test_physical_usb_binding.py -v
Ran 9 tests in 0.010s -> OK

# 3. اختبار تكيف الشاشات المتعددة (800x480 إلى 4K)
python3 scratch/test_multi_resolution_adaptation.py
Ran 9 tests across 9 industrial resolutions -> 100% PASS

# 4. اختبار ثبات التخطيط وهندسة الـ ROI النسبية
python3 scratch/test_roi_and_layout_stability.py
Ran 5 tests -> 100% PASS
```

---

## 6. الإصدار الموحد النهائي (Version 3.0 Gold Release)

يمثل الإصدار **Version 3.0 Gold Release** قمة النضج الهندسي للمنظومة، حيث دمج أفضل المزايا التقنية من النسخة المحلية ومستودع GitHub في بنية موحدة متماسكة:

1. **الربط المزدوج للريلاي (Dual Relay Mapping A & B)**:
   - دعم كامل في `hardware_worker.py` و `hardware_manager.py` لتمكين كل محطة من إطلاق ريليهين في آنٍ واحد (مثل إنذار ضوئي على ريليه 1 وتوقف آلي على ريليه 3)، مع تغطية كاملة بـ 16 اختبار وحدة آلي.
2. **محرك التشخيص والصحة النمطي (`health_diagnostics.py`)**:
   - رصد حي ومستمر لموارد المعالج، والذاكرة العشوائية، وحرارة اللوحة، وحساب الـ FPS الفعلي ومعدل سقوط الإطارات (Dropped Frames) بدقة متناهية.
3. **نظام التقارير الذكية وتوليد PDF (`report_manager.py` & `reporting_page.py`)**:
   - توليد تقارير أمان متقدمة بصيغة PDF تتضمن صور الكاميرات الملتقطة لحظة اختراق منطقة الخطر وإحصائيات الورديات.
4. **خدمة التنبيهات البريدية التلقائية (`email_manager.py`)**:
   - إرسال التنبيهات الفورية وتقارير الورديات عبر بروتوكول SMTP مع طابور إرسال محلي (`EmailQueue`) لحفظ الرسائل عند انقطاع الشبكة وإعادة إرسالها تلقائياً.
5. **إدارة التخزين والأرشفة الشهرية (`storage_manager.py`)**:
   - سياسات تنظيف ذكية وضغط الصور والسجلات في ملفات مضغوطة شهرياً لمنع امتلاء مساحة القرص.
6. **لوحة المفاتيح الافتراضية ثنائية اللغة (`osk_widget.py`)**:
   - دعم التبديل السلس بين **اللغتين العربية والإنجليزية**، مع مفاتيح `Qt.NoFocus` ونوافذ حوار آمنة لمسياً (`TouchPasswordDialog`).
7. **الربط الفيزيائي لمنافذ الـ USB للكاميرات (`Physical USB Topology Binding`)**:
   - منع تبديل الكاميرات نهائياً عبر القفل على المنفذ الفيزيائي في المذربورد (`1-1.2`، إلخ).
8. **الإحداثيات النسبية المعيارية (`Normalized ROI 0.0 - 1.0`)**:
   - ثبات هندسي مطلق لحدود مناطق الخطر وشبكة العرض عبر مختلف دقات الشاشات الصناعية.

**الخلاصة:** المنظومة بكافة ملفاتها البرمجية والتوثيقية باتت في حالة اتساق تام ومحصنة صناعياً وجاهزة للتشغيل الميداني المستمر والرفع إلى مستودع GitHub.

