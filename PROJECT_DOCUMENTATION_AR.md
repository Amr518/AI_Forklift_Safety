# توثيق شامل لنظام أمان الرافعات الشوكية بالذكاء الاصطناعي
# Limitless Future — AI Forklift Safety System
### الدليل المرجعي الفني المتكامل للمطورين وفرق الصيانة والتشغيل
**إصدار الوثيقة:** 3.0 (الإصدار الذهبي الموحد - Production Gold Release) | **تاريخ التوثيق:** سبتمبر 2026 | **نظام التشغيل المستهدف:** Ubuntu 24.04 LTS

---

## 📑 فهرس المحتويات
1. [نظرة عامة على المشروع وأهدافه](#1-نظرة-عامة-على-المشروع-وأهدافه)
2. [المعمارية العامة وتدفق البيانات (System Architecture)](#2-المعمارية-العامة-وتدفق-البيانات-system-architecture)
3. [دليل المسارات ومواقع الملفات في النظام (System Paths)](#3-دليل-المسارات-ومواقع-الملفات-في-النظام-system-paths)
4. [الهيكل الشجري الكامل للمشروع (Directory Tree)](#4-الهيكل-الشجري-الكامل-للمشروع-directory-tree)
5. [الشرح التفصيلي لجميع الملفات البرمجية والأكواد (ملفاً بملف)](#5-الشرح-التفصيلي-لجميع-الملفات-البرمجية-والأكواد)
   - [5.1 ملفات الإقلاع وإعدادات النظام (Shell Scripts)](#51-ملفات-الإقلاع-وإعدادات-النظام-shell-scripts)
   - [5.2 ملفات الإعدادات وتخزين البيانات (Configuration & State)](#52-ملفات-الإعدادات-وتخزين-البيانات-configuration--state)
   - [5.3 النواة البرمجية الرئيسية (Root System Modules)](#53-النواة-البرمجية-الرئيسية-root-system-modules)
   - [5.4 الحزمة المعيارية المنظمة (Modular Subpackage: forklift_ai_safety)](#54-الحزمة-المعيارية-المنظمة-forklift_ai_safety)
   - [5.5 ملفات الاختبارات والتحقق (Test Suites)](#55-ملفات-الاختبارات-والتحقق-test-suites)
6. [بروتوكول التواصل بين العمليات (IPC Protocol Details)](#6-بروتوكول-التواصل-بين-العمليات-ipc-protocol-details)
7. [التحكم بالعتاد الفيزيائي ومصفوفة الريلاي (Hardware & Relay Logic)](#7-التحكم-بالعتاد-الفيزيائي-ومصفوفة-الريلاي-hardware--relay-logic)
8. [منظومة الأمان والصلاحيات (Role-Based Access Control)](#8-منظومة-الأمان-والصلاحيات-role-based-access-control)
9. [دليل التشغيل، الصيانة واستكشاف الأخطاء (Troubleshooting Guide)](#9-دليل-التشغيل-الصيانة-واستكشاف-الأخطاء-troubleshooting-guide)
10. [الترقيات والتحسينات الإنتاجية الحديثة (Production Upgrades & Industrial Resilience)](#10-الترقيات-والتحسينات-الإنتاجية-الحديثة-production-upgrades)
    - [10.1 الربط الفيزيائي لمنافذ USB ومنع تبديل الكاميرات عند الاهتزاز](#101-الربط-الفيزيائي-لمنافذ-usb-ومنع-تبديل-الكاميرات-عند-الاهتزاز)
    - [10.2 التحكم والربط اللحظي للريلاي لكل محطة وزر الفحص المباشر](#102-التحكم-والربط-اللحظي-للريلاي-لكل-محطة-وزر-الفحص-المباشر)
    - [10.3 إدارة الخدمة والاسترداد الذاتي عبر Systemd وحل تضارب التشغيل](#103-إدارة-الخدمة-والاسترداد-الذاتي-عبر-systemd-وحل-تضارب-التشغيل)
    - [10.4 مهلة استقرار العتاد ومنافذ الـ USB عند الإقلاع (15s Startup Delay)](#104-مهلة-استقرار-العتاد-ومنافذ-الـ-usb-عند-الإقلاع-15s-startup-delay)
    - [10.5 حماية شبكة العرض من التمدد والتكبير عند انقطاع الكاميرات](#105-حماية-شبكة-العرض-من-التمدد-والتكبير-عند-انقطاع-الكاميرات)
    - [10.6 نظام الإحداثيات النسبية المعيارية لمربع الـ ROI (0.0 - 1.0)](#106-نظام-الإحداثيات-النسبية-المعيارية-لمربع-الـ-roi-00---10)
    - [10.7 التكيف الديناميكي مع أي نوع وأي مقاس شاشة (Responsive UI: 800x480 to 4K)](#107-التكيف-الديناميكي-مع-أي-نوع-وأي-مقاس-شاشة-responsive-ui-800x480-to-4k)
11. [سجل معالجة العيوب والتحصين البرمجي المكتمل (Defect Remediation & Hardening Log)](#11-سجل-معالجة-العيوب-والتحصين-البرمجي-المكتمل)

---

## 1. نظرة عامة على المشروع وأهدافه

### 1.1 الغرض والرسالة
مشروع **Limitless Future — AI Forklift Safety System** هو منظومة صناعية فورية (Real-time Industrial Safety System) مصممة لحماية العاملين في بيئات المستودعات ومصانع الأدوية (وفق معايير **Pharma 4.0**).
الهدف المحوري للنظام هو **منع حوادث اصطدام الرافعات الشوكية بالعمال والمارة** من خلال:
1. مراقبة مسارات ومحيط الرافعات الشوكية بواسطة ما يصل إلى **4 كاميرات صناعية** في آنٍ واحد.
2. الكشف الآلي الفوري عن وجود الأشخاص باستخدام شبكة عصبية عميقة تعتمد على نموذج **YOLOv8**.
3. التحقق مما إذا كان الشخص المكتشف قد دخل في منطقة الخطر المحددة مسبقاً (**Region of Interest - ROI**).
4. إرسال إشارة كهربائية فيزيائية عبر بطاقة ترحيل رباعية القنوات (**RM04U USB Relay**) لإطلاق سارينة إنذار، وميض ضوئي، أو إيقاف طوارئ فوري للرافعة.

### 1.2 التقنيات المستخدمة (Technology Stack)
- **نظام التشغيل:** Linux Ubuntu 24.04 LTS (بيئة X11 Display Server).
- **لغة البرمجة:** Python 3.10+ مع عزل كامل عبر Virtual Environment (`venv`).
- **الواجهة الرسومية:** PyQt5 (إطارات مخصصة، شاشات لمس Kiosk Mode، رسم متجهات بالزمن الفعلي).
- **الذكاء الاصطناعي والرؤية الحاسوبية:** 
  - Ultralytics YOLOv8 (نموذج `yolov8n.pt` فائق الخفة والمُحسَّن للمعالجات المركزية).
  - OpenCV (`cv2`) لمعالجة وتحويل إطارات الفيديو ومعدل التدفق V4L2.
  - PyTorch كخلفية استدلال ذكاء اصطناعي.
- **التواصل الداخلي (IPC):** مقابس يونكس الموضعية (Unix Domain Sockets) على القرص فائق السرعة `/dev/shm`.
- **التواصل مع العتاد الفيزيائي:** PySerial للتخاطب مع شريحة CH340 بمعدل 9600 باود وأوامر ASCII.

---

## 2. المعمارية العامة وتدفق البيانات (System Architecture)

يعتمد النظام معمارية العمليتين المنفصلتين (**Two-Process Architecture**) لعزل واجهة المستخدم والذكاء الاصطناعي تماماً عن التحكم في العتاد الفيزيائي لضمان عدم توقف دوائر الأمان في حال تجمد الواجهة أو العكس:

```
┌──────────────────────────────────────────────────────────────────────────┐
│             العملية الأولى: العملية الرسومية والذكاء الاصطناعي           │
│                   PROCESS 1: ai_gui_system.py (GUI & AI)                 │
│                                                                          │
│  ┌────────────────────┐   ┌────────────────────┐   ┌──────────────────┐  │
│  │ CameraWorkerThread │   │   YoloDetector     │   │   AlarmSystem    │  │
│  │ (4 QThreads)       │──►│   (YOLOv8 Nano)    │──►│ (ROI Evaluation) │  │
│  └────────────────────┘   └────────────────────┘   └──────────────────┘  │
│            ▲                                                 │           │
│            │ (V4L2 Frames)                                   ▼           │
│    كاميرات المراقبة                                    USBRelayManager   │
│   (/dev/video0..6)                                    (IPC Socket Client)│
└──────────────────────────────────────────────────────────────┬───────────┘
                                                               │
                         أوامر بصيغة JSON عبر مقبس يونكس        │ JSON-over-newline
                         /dev/shm/forklift_relay.sock          │
                                                               ▼
┌──────────────────────────────────────────────────────────────────────────┐
│              العملية الثانية: خادم التحكم بالعتاد والريلاي               │
│               PROCESS 2: hardware_worker.py (Hardware Daemon)            │
│                                                                          │
│  ┌────────────────────┐   ┌────────────────────┐   ┌──────────────────┐  │
│  │     IPCServer      │   │    SharedState     │   │  HardwareWorker  │  │
│  │ (UDS Socket Server)│──►│ (Thread-Safe Store)│──►│  (OR-Gate Logic) │  │
│  └────────────────────┘   └────────────────────┘   └──────────────────┘  │
│                                                              │           │
│                                           أوامر تسلسلية عبر  │ N1..N4    │
│                                          /dev/forklift_relay │ F1..F4    │
│                                                              ▼           │
│                                            لوحة الترحيل RM04U 4-Ch Relay │
│                                             (تشغيل السارينة / الطوارئ)   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.1 تدفق معالجة الإطار (Data Processing Pipeline)
1. **التقاط الإطار:** تقوم خيوط `CameraWorkerThread` المستقلة بقراءة الإطار من منفذ الكاميرا (`/dev/videoX`).
2. **الجدولة والتخطي الذكي:** لتخفيف العبء عن المعالج (CPU Optimization)، يتم تطبيق استدلال الذكاء الاصطناعي كل 3 إطارات (Frame Skipping)، مع عرض كل الإطارات بسلاسة للمشغل.
3. **الاستدلال (Inference):** يقوم `YoloDetector` بالبحث عن الكائنات وتصفية فئة المشاة فقط (`Class 0: person`).
4. **فحص الحدود (ROI Inspection):** يحسب `AlarmSystem` نقطة المركز السفلي للشخص `((x1+x2)/2, (y1+y2)/2)` ويتحقق إذا كانت داخل منطقة الأمان `QRect`.
5. **إرسال الأمر:** يرسل العميل `USBRelayManager` أمر JSON إلى مقبس الخادم.
6. **بوابة OR الفيزيائية:** يقوم خادم العتاد `HardwareWorker` بتطبيق معادلة التخفيض المنطقية لجمع إنذارات المحطات المتعددة المربوطة بنفس قناة الريلاي وإصدار أمر الإغلاق الفيزيائي.

---

## 3. دليل المسارات ومواقع الملفات في النظام (System Paths)

| نوع المسار | المسار في النظام | الوصف والدور الفني |
|---|---|---|
| **مسار المشروع** | `/home/forklift/Desktop/AI_Forklift_Safety` | المجلد الأساسي الذي يحتوي على كافة الشيفرات والملفات التنفيذية. |
| **مقبس الاتصال (IPC)** | `/dev/shm/forklift_relay.sock` | مقبس يونكس موجود داخل ذاكرة الرام المشتركة (`tmpfs`) لتحقيق سرعة استجابة فائقة (أقل من 1 ملي ثانية). |
| **منفذ الريلاي الفيزيائي** | `/dev/forklift_relay` | رابط رمزي دائم (Persistent Symlink) يتم إنشاؤه عبر udev ليشير إلى جهاز RM04U. |
| **منفذ الريلاي البديل** | `/dev/ttyUSB0` | المنفذ التسلسلي الافتراضي لشريحة CH340 في نظام لينكس. |
| **منافذ الكاميرات** | `/dev/video0`, `/dev/video2`, `/dev/video4`, `/dev/video6` | عقد أجهزة الفيديو V4L2 للكاميرات الصناعية الأربع (الأرقام الزوجية للبث الفعلي). |
| **ملف قفل الأمان** | `/tmp/hardware_worker.lock` | ملف قفل فردي لمنع تشغيل أكثر من نسخة واحدة من خادم العتاد في نفس اللحظة. |
| **ملف الإعدادات** | `config.json` | ملف الإعدادات الشامل (إحداثيات المناطق، سرعة الاتصال، توزيع الكاميرات والريلاي). |
| **ملف الصلاحيات** | `security.json` | كلمات المرور وأدوار المستخدمين الخمسة. |
| **سجل العتاد** | `hardware.log` | سجل تشخيصي دوري يسجل حالات الاتصال وأوامر الريلاي. |
| **نموذج الذكاء الاصطناعي** | `yolov8n.pt` | أوزان شبكة YOLOv8 Nano للتعرف على الأشخاص. |
| **بيئة بايثون الافتراضية** | `/home/forklift/Desktop/AI_Forklift_Safety/venv` | بيئة معزولة تحتوي على الحزم والمكتبات المحددة. |

---

## 4. الهيكل الشجري الكامل للمشروع (Directory Tree)

```text
AI_Forklift_Safety/
├── .gitignore                             # قواعد استبعاد الملفات المؤقتة والبيئات الافتراضية لـ GitHub
├── AI_Forklift_System.desktop             # اختصار تشغيل النظام من سطح مكتب أوبونتو
├── PROJECT_FULL_DOCUMENTATION.md          # الدليل الفني المفصل باللغة الإنجليزية
├── PROJECT_DOCUMENTATION_AR.md            # هذا الملف: المرجع الشامل باللغة العربية
├── PROJECT_AUDIT_AND_BUGS_REFERENCE_AR.md # سجل التدقيق الهندسي الشامل والعيوب المعالجة
├── README.md                              # واجهة التوثيق الرئيسية لـ GitHub
├── UBUNTU_MIGRATION_GUIDE.md              # دليل التثبيت والتشغيل الصناعي على Ubuntu 24.04 LTS
├── industrial_safety_review_report.txt    # تقرير التدقيق والمعايرة الصناعية
├── requirements.txt                       # قائمة متطلبات ومكتبات بايثون الإنتاجية النقية
├── setup_linux.sh                         # سكريبت تثبيت وإعداد بيئة لينكس كاملة
├── setup_usb.sh                           # سكريبت إنشاء قواعد udev وصلاحيات USB
├── start_system.sh                        # سكريبت التشغيل الأساسي للنظام (مع مهلة الـ 15 ثانية)
├── config.json                            # ملف إعدادات النظام، الكاميرات، الريلاي والـ ROI النسبي
├── security.json                          # ملف كلمات المرور والأدوار الأمنية
├── relay_status.json                      # حالة الريلاي الحالية المحفوظة
├── relay_statusadmin.json                 # نسخة احتياطية إدارية لحالات الريلاي
├── hardware.log                           # ملف سجل عمليات العتاد والاتصال التسلسلي
├── yolov8n.pt                             # ملف أوزان نموذج YOLOv8 Nano
│
├── ai_gui_system.py                 # [العملية 1] واجهة المستخدم الرسومية والذكاء الاصطناعي (Responsive)
├── hardware_worker.py               # [العملية 2] خادم العتاد والريلاي وبوابة OR
├── hardware_manager.py              # وسيط العميل للاتصال بالعتاد عبر Socket IPC
├── camera.py                        # وحدة إدارة وتشغيل كاميرا OpenCV
├── detector.py                      # وحدة استدعاء واستدلال نموذج YOLOv8
├── alarm.py                         # نظام تقييم مناطق الخطر وإصدار التحذير
├── roi.py                           # ويدجت رسم وتعديل منطقة الخطر بنظام الإحداثيات النسبية
├── security.py                      # مدير المصادقة والتحقق من الصلاحيات
├── osk_widget.py                    # لوحة المفاتيح الافتراضية المدمجة لشاشات اللمس
├── test_or_gate_logic.py            # حزمة الاختبارات الآلية لمنطق العتاد وبوابة OR
│
├── scratch/                         # حزم الاختبارات والتحقق البرمجي المتقدم
│   ├── test_card_relay_and_camera_dropdown.py # اختبار ربط ومزامنة الريلاي والكاميرات لكل محطة
│   ├── test_physical_usb_binding.py           # اختبار تثبيت منافذ USB الفيزيائية ومنع التبديل
│   ├── test_production_architecture.py        # اختبار الأمان وبنية الـ IPC والـ OSK
│   ├── test_roi_and_layout_stability.py       # اختبار ثبات شبكة العرض والـ ROI النسبي
│   └── test_multi_resolution_adaptation.py   # اختبار تكيف الواجهة عبر 9 مقاسات شاشات مختلفة
│
├── ~/.config/systemd/user/
│   └── forklift-ai.service          # خدمة إدارة واسترداد النظام التلقائي (Systemd Daemon)
├── ~/.config/autostart/
│   └── AI_Forklift_Safety.desktop   # بدء التشغيل التلقائي عند إقلاع الجلسة بدون تضارب
└── ~/Desktop/run_system.sh          # مشغل النظام المباشر عبر أمر Systemd
│
└── forklift_ai_safety/              # الحزمة البرمجية المعيارية المقسمة (Subpackage)
    ├── main.py                      # نقطة الدخول للحزمة المعيارية
    ├── config/
    │   └── security.json            # إعدادات الأمان الخاصة بالحزمة
    ├── core/                        # النواة الوظيفية المنفصلة
    │   ├── __init__.py
    │   ├── camera.py                # فئة الكاميرا المعيارية
    │   ├── camera_thread.py         # خيط تشغيل الكاميرا بالخلفية
    │   ├── detector.py              # فئة كاشف المشاة المعيارية
    │   ├── roi_manager.py           # مدير حسابات وتخزين مناطق الخطر
    │   ├── alarm_manager.py         # مدير تشغيل الإنذارات والأصوات
    │   └── security.py              # مدير الصلاحيات المعياري
    └── ui/                          # واجهات المستخدم المعيارية
        ├── __init__.py
        ├── main_window.py           # النافذة الرئيسية المعيارية
        ├── camera_panel.py          # لوحة عرض الكاميرا الفردية
        ├── video_panel.py           # شبكة عرض الكاميرات المجمعة
        ├── sidebar_panel.py         # شريط الأدوات والتحكم الجانبي
        └── status_bar.py            # شريط الحالة السفلي
```

---

## 5. الشرح التفصيلي لجميع الملفات البرمجية والأكواد

### 5.1 ملفات الإقلاع وإعدادات النظام (Shell Scripts)

#### 1. `start_system.sh` (سكريبت التشغيل المتسلسل الرئيسي)
- **الوظيفة:** الملف التنفيذي المسؤول عن تشغيل النظام الصناعي كاملاً بالترتيب الصحيح وضمان عدم وجود عمليات يتيمة أو تضارب في الموارد.
- **آلية العمل خطوة بخطوة:**
  1. **مهلة استقرار العتاد (15 ثانية):** عد تنازلي مدته 15 ثانية في بداية التشغيل لإعطاء النواة ومتحكمات الـ USB والموزعات (Hubs) وقنوات V4L2 ومتحكم السيريال CH340 فرصة كاملة لتثبيت العقد `/dev/video*` و `/dev/ttyUSB0` واستقرار التغذية الكهربائية.
  2. **راية التخطي السريع للمطورين:** إمكانية تشغيل السكريبت بدون تأخير أثناء أعمال الصيانة عبر تمرير `./start_system.sh --no-delay` أو `SKIP_DELAY=1`.
  3. **التعرف التلقائي على بيئة العرض:** الكشف الذكي عن مسار ملف الصلاحية `XAUTHORITY` (مثل `/run/user/1001/gdm/Xauthority`) ومتغير `DISPLAY` ومنح صلاحيات الخادم الرسومي عبر `xhost`.
  4. **تنظيف العمليات والمقابس القديمة:** إنهاء أي عملية يتيمة لخادم العتاد (`pkill -f hardware_worker.py`) وإزالة ملفات القفل والمقابس القديمة من الذاكرة المشتركة (`/dev/shm/*.sock`, `/tmp/*.lock`).
  5. **تفعيل البيئة الافتراضية:** `source venv/bin/activate` داخل مجلد المشروع.
  6. **تشغيل خادم العتاد في الخلفية:** `python3 hardware_worker.py &` والاحتفاظ برقم العملية `HW_PID`.
  7. **التحقق التكراري من جاهزية المقبس:** (Polling كل 0.5 ثانية حتى 15 ثانية) بما يغطي فترة فحص الإقلاع الذاتي للريلاي (Self-Test).
  8. **تشغيل واجهة المستخدم الرسومية:** بوضع ملء الشاشة الصناعي Kiosk Mode عبر `export QT_QPA_PLATFORM=xcb && python3 ai_gui_system.py`.
  9. **الإغلاق الآمن وتمرير كود الخروج:** عند إغلاق الواجهة، ينهي السكريبت خادم العتاد ويمرر كود الخروج الفعلي ليتسنى لـ Systemd اتخاذ قرار إعادة التشغيل التلقائي.

#### 2. `forklift-ai.service` (خدمة إدارة واسترداد النظام التلقائي عبر Systemd)
- **المسار:** `~/.config/systemd/user/forklift-ai.service`
- **الوظيفة:** إدارة دورة حياة المنظومة على مستوى نواة النظام كـ Production Daemon.
- **الخصائص التشغيلية:**
  - `Restart=always`: إعادة التشغيل التلقائي الفوري للنظام في حال توقفه أو حدوث كراش غير متوقع أو إغلاقه عن طريق الخطأ.
  - `RestartSec=3`: زمن الانتظار قبل إعادة الإقلاع هو 3 ثوانٍ فقط لتقليل فترة انقطاع المراقبة لأدنى حد صناعي ممكن.
  - `TimeoutStartSec=60`: مهلة كافية تستوعب مهلة الـ 15 ثانية لاستقرار العتاد دون إطلاق إنذار توقف من Systemd.
  - `KillMode=mixed` و `TimeoutStopSec=5`: إنهاء نظيف يمنع بقاء أي عمليات ميتة أو مقابس عالقة في الذاكرة.
  - **ميزة الـ Lingering:** تم تفعيل `loginctl enable-linger forklift` لتعمل الخدمة تلقائياً مع إقلاع الجهاز فوراً.

#### 3. `run_system.sh` و `AI_Forklift_Safety.desktop` (منع تضارب الإقلاع التلقائي)
- **الوظيفة:** تم تحويل أمر التشغيل التلقائي في النظام وأيقونات سطح المكتب لتوجه أمرها إلى Systemd مباشرة:
  ```bash
  systemctl --user restart forklift-ai.service
  ```
  هذا التوجيه الموحد يمنع حدوث Race Condition أو تشغيل نسختين في نفس الوقت تتنافسان على منفذ الريلاي التسلسلي والكاميرات.

#### 2. `setup_linux.sh` (تهيئة خادم وبيئة لينكس)
- **الوظيفة:** إعداد وتجهيز نظام Ubuntu لأول مرة لتشغيل النظام بدون مشاكل صلاحيات أو نقص في المكتبات المشتركة.
- **المهام المنفذة:**
  - تثبيت مكتبات النظام الرسومية والفيديو: `libgl1-mesa-glx`, `libxcb-*`, `v4l-utils`.
  - إضافة المستخدم الحالي إلى مجموعات الوصول للعتاد: `dialout` (للمنافذ التسلسلية) و `video` (لكاميرات USB) حتى يعمل النظام بدون الحاجة لـ `sudo`.
  - بناء البيئة الافتراضية وتثبيت الحزم من `requirements.txt`.
  - استدعاء سكريبت ضبط الـ udev.

#### 3. `setup_usb.sh` (إعداد قواعد منافذ الـ USB)
- **الوظيفة:** يكتب ملف القواعد `/etc/udev/rules.d/99-forklift-relay.rules`.
- **المحتوى:** يقوم بتعيين رابط دائم باسم `/dev/forklift_relay` عند اكتشاف شريحة USB التسلسلية ذات المعرف (Vendor ID: `1a86`, Product ID: `7523` الخاصة بـ CH340)، مع منح الصلاحية `MODE="0666"`.

#### 4. `AI_Forklift_System.desktop` (أيقونة سطح المكتب)
- **الوظيفة:** ملف قياسي لنظام XDG لتمكين المشغلين في المصنع من تشغيل النظام بمجرد لمس الأيقونة على سطح المكتب، حيث يقوم بتشغيل `start_system.sh`.

---

### 5.2 ملفات الإعدادات وتخزين البيانات (Configuration & State)

#### 1. `config.json` (ملف التكوين العام)
يحتوي على قسمين رئيسيين:
1. **إحداثيات الكاميرات ومناطق الخطر (`camera_0` إلى `camera_3`):**
   - **نظام الإحداثيات النسبية المعيارية (`norm_x`, `norm_y`, `norm_w`, `norm_h`):** تُحفظ كنسب مئوية بين `0.0` و `1.0` لضمان بقاء مربع الـ ROI ثابتاً في موقعه الفيزيائي الدقيق مهما تغيرت دقة الشاشة أو أبعاد النافذة أو انقطعت الكاميرا.
   - **القيم البكسلية (`x, y, w, h`):** قيم مرجعية إرشادية متوافقة عكسياً.
   - **مسار منفذ USB الفيزيائي (`physical_usb_path`):** لتثبيت الكاميرا بالمنفذ الفيزيائي في اللوحة الأم ومنع تبديل الكاميرات عند الاهتزاز.
2. **قسم الإعدادات (`settings`):**
   - `com_port`: المنفذ التسلسلي للريلاي (`/dev/ttyUSB0` أو `/dev/forklift_relay`).
   - `baud_rate`: معدل نقل البيانات (افتراضياً `9600`).
   - `camera_mapping`: مصفوفة المنافذ أو العقد الافتراضية للكاميرات.
   - `camera_physical_paths`: مصفوفة المسارات الفيزيائية للأجهزة الأربعة (مثل `["1-1.2", "1-1.4", null, null]`).
   - `relay_mapping`: مصفوفة تربط كل محطة كاميرا بقناة ريلاي فيزيائية من 1 إلى 4 (مثال: `[2, 2, 4, 1]` لربط المحطة 1 بالريلاي 2، والمحطة 3 بالريلاي 4، إلخ).
   - `model_path`: مسار نموذج الذكاء الاصطناعي (`yolov8n.pt`).
   - `confidence`: نسبة الثقة المطلوبة لاعتماد اكتشاف الشخص (افتراضياً `0.35`).
   - `on_delay`: زمن التأخير قبل تفعيل الريلاي لمنع الإنذارات الكاذبة اللحظية.
   - `off_delay`: زمن استمرار تشغيل الريلاي بعد خروج الشخص لضمان سلامة الرافعة (مثلاً `10.0` ثوانٍ).
   - `boot_self_test`: تفعيل أو إلغاء فحص الريلاي عند الإقلاع (`1` مفعل).
   - `hardware_coil_enabled`: مفتاح قطع برمجية يعطل الإشارة الكهربائية لجميع القنوات عند الحاجة.

#### 2. `security.json` (ملف الصلاحيات وكلمات المرور)
يخزن مصفوفة الصلاحيات الخماسية:
```json
{
    "Operator": "",
    "Technician": "tech",
    "Supervisor": "1111",
    "Engineer": "admin",
    "Administrator": "superadmin"
}
```
- المشغل (`Operator`) لا يحتاج لكلمة مرور، ولديه صلاحيات مراقبة فقط دون إمكانية التعديل أو الخروج من النظام.

---

### 5.3 النواة البرمجية الرئيسية (Root System Modules)

#### 1. `detector.py` (كاشف المشاة والذكاء الاصطناعي)
- **الفئة الرئيسية:** `YoloDetector`
- **الوظيفة:** تغليف مكتبة Ultralytics وتحميل نموذج YOLOv8 لتنفيذ الاستدلال السريع.
- **أهم الدوال والمتغيرات:**
  - `__init__(self, model_name="yolov8n.pt", confidence=0.25)`: يبحث عن ملف النموذج في المسار الحالي أو المسار الأب، وفي حال عدم وجوده يتم تنزيله تلقائياً، ثم يقوم بتهيئة كائن `YOLO`.
  - `detect_persons(self, frame, conf=None, imgsz=320)`: 
    - تأخذ إطار الصورة `frame` (مصفوفة NumPy BGR).
    - تنفذ النموذج مع تصغير حجم الإدخال إلى `imgsz=320` لتسريع المعالجة على المعالجات المركزية.
    - تفحص نتائج الكشف وتستخرج الكائنات التي تتبع الفئة `cls == 0` فقط (والتي تمثل الإنسان/الشخص في تصنيف COCO).
    - تعيد قائمة بإحداثيات الصناديق المحيطة `[(x1, y1, x2, y2), ...]`.

#### 2. `camera.py` (إدارة كاميرات الفيديو)
- **الفئة الرئيسية:** `Camera`
- **الوظيفة:** إدارة الاتصال المباشر بجهاز الكاميرا عبر OpenCV وتوفير عزل للأخطاء.
- **أهم الدوال:**
  - `initialize_camera(self)`: تفتح المنفذ المحدد (`device_index`) وتضبط الأبعاد على 640×480 بكسل.
  - `read_frame(self)`: تلتقط الإطار الحالي. وفي حال فشل القراءة أو فصل الكاميرا، تعيد `(False, None)` لمنع انهيار البرنامج.
  - `release(self)`: تحرير منفذ الكاميرا وموارد العتاد عند الإغلاق.

#### 3. `roi.py` (إدارة ورسم منطقة الأمان بنظام الإحداثيات النسبية المعيارية)
- **الفئة الرئيسية:** `VideoLabel` (ترث من `QLabel`)
- **الوظيفة:** ويدجت رسومي فائق التطور والتحصين لعرض فيديو الكاميرا ورسم وتعديل منطقة الخطر (ROI) مع ضمان الثبات المطلق للأبعاد وموضع الصندوق.
- **المميزات والتحصينات البرمجية المطبقة:**
  - **نظام الإحداثيات النسبية الموحدة (Normalized Coordinates):**
    تحويل الإحداثيات إلى نسب مئوية معيارية `norm_roi = (norm_x, norm_y, norm_w, norm_h)` في المدى `[0.0, 1.0]`. هذا يضمن بقاء المربع محكماً وثابتاً في موقعه الفيزيائي الدقيق على الشاحنة مهما تغير مقاس الشاشة أو تم تدويرها أو تكبيرها أو تصغيرها.
  - **التكيف التلقائي مع المقاسات (`resizeEvent`):**
    إعادة حساب الإحداثيات البكسلية `roi_rect` لحظياً عند أي حدث تغيير في حجم النافذة (`resizeEvent`) استناداً إلى النسب النسبية، مما يمنع انزياح الصندوق نهائياً.
  - **عزل شبكة العرض وثبات الحجم (`sizeHint` و `QSizePolicy.Ignored`):**
    تخصيص دالتي `sizeHint` و `minimumSizeHint` لإرجاع أبعاد قياسية ثابتة (`320x240` و `160x120`) بصرف النظر عن حجم الصورة المعروضة. وبفضل تعيين سياسة الحجم إلى `QSizePolicy.Ignored`، يتم عزل شبكة العرض 2x2 تماماً عن تقلبات دقة إطارات الكاميرات أو لوحات التنبيه، مما يقضي على ظاهرة التمدد (Stretch) عند انقطاع أي كاميرا.
  - **الدوال المعيارية:** `set_norm_roi()` لتعيين الإحداثيات النسبية، و `get_norm_roi()` لاسترجاعها، و `set_roi()` المتوافقة عكسياً مع الإحداثيات البكسلية.
  - معالجة أحداث الفأرة واللمس: `mousePressEvent`, `mouseMoveEvent`, `mouseReleaseEvent` للرسم والسحب وتغيير الحجم، ومقابض الزوايا الأربع (`get_handles`).

#### 4. `alarm.py` (منطق تقييم الأمان والإنذار)
- **الفئة الرئيسية:** `AlarmSystem`
- **الوظيفة:** المقارنة الهندسية بين مواقع الأشخاص المكتشفين وحدود منطقة الخطر.
- **أهم الدوال:**
  - `evaluate_safety(self, persons, roi_rect)`: 
    لكل شخص مكتشف، يتم استخراج نقطة المنتصف:
    $$\text{center\_x} = \frac{x_1 + x_2}{2}, \quad \text{center\_y} = \frac{y_1 + y_2}{2}$$
    ويتم فحص وقوع هذه النقطة داخل المستطيل عبر `roi_rect.contains(QPoint(center_x, center_y))`. في حال وجود أي شخص داخل المستطيل تعيد `True` (حالة خطر).
  - `get_status_styles(self, warning)`: تعيد النصوص وأكواد الألوان التنسيقية للواجهة (أحمر للتحذير وأخضر للأمان).

#### 5. `security.py` (إدارة الأمان والتحقق)
- **الفئة الرئيسية:** `SecurityManager`
- **الوظيفة:** قراءة وتحديث ملف `security.json` والتحقق من صحة كلمات المرور للأدوار المختلفة.
- **أهم الدوال:**
  - `verify_password(self, role, password)`: مقارنة كلمة المرور المدخلة بالمسجلة للدور المطلوب.
  - `change_password(self, role, new_password)`: تحديث كلمة المرور وحفظها بالملف فوراً.

#### 6. `osk_widget.py` (لوحة المفاتيح الافتراضية على الشاشة)
- **الفئة الرئيسية:** `OSKWidget` و `VirtualKeyboard`
- **الوظيفة:** لوحة مفاتيح متطورة مدمجة صُممت خصيصاً لأجهزة المصانع التي تعمل بشاشات لمس دون وجود لوحة مفاتيح فيزيائية.
- **الخصائص الفنية الهامة:**
  - خاصية **NoFocus (`Qt.NoFocus`)**: جميع أزرار اللوحة تحمل هذه الخاصية حتى لا تسحب التركيز من حقل الإدخال المستهدف (`QLineEdit`).
  - دعم اللغتين: تدعم التبديل اللحظي بين الإنجليزية القياسية (QWERTY) والعربية (مطابقة لتوزيعة Windows 101).
  - حقن المفاتيح: تستخدم دالتي `.insert()` و `.backspace()` الخاصة بحقول إدخال Qt مما يجعلها متوافقة تماماً مع حقول كلمات المرور المشفرة وحقول النصوص العادية.

#### 7. `hardware_manager.py` (عميل الـ IPC للعتاد)
- **الفئة الرئيسية:** `USBRelayManager`
- **الوظيفة:** يمثل جانب العميل (IPC Client) داخل عملية الواجهة الرسومية؛ حيث يقوم بإنشاء اتصال مقبس مستمر مع خادم العتاد `hardware_worker.py`.
- **أهم الخصائص والوظائف:**
  - **إعادة الاتصال اللانهائي (`_connect_socket`):** يعمل في خيط خلفي مستقل ويعاود الاتصال كل 0.5 ثانية دون استسلام في حال إعادة تشغيل الخادم.
  - `trigger_relay(self, station, state, manual=False)`: إرسال أمر تفعيل أو إيقاف لمحطة معينة بصيغة JSON.
  - `update_config(...)`: إرسال تحديثات أزمنة التأخير وتفعيل الملف الكهربائي إلى الخادم.
  - `set_relay_mapping(self, mapping)`: تحديث مصفوفة ربط الكاميرات بقنوات الريلاي.
  - `test_relay(self, channel, state)`: إرسال أمر اختبار يدوي لقناة ريلاي محددة من شاشة الإعدادات أو بطاقة المحطة.

#### 8. `hardware_worker.py` (خادم العتاد ومنطق بوابة OR)
- **الوظيفة:** العملية الخلفية المستقلة (Hardware Daemon) المسؤولة حصرياً عن التحكم بالمنفذ التسلسلي وبطاقة الترحيل الفيزيائية.
- **الفئات الرئيسية:**
  1. `SharedState`: مخزن بيانات آمن متعدد الخيوط (Thread-Safe Data Store) محمي بواسطة أقفال `threading.Lock` لتخزين حالات المحطات والريلاي والإعدادات.
  2. `IPCServer`: خادم مقابس يونكس يستمع على `/dev/shm/forklift_relay.sock` ويستقبل اتصالات الواجهة الرسومية ويفك رسائل JSON الواردة ويحدث `SharedState`.
  3. `HardwareWorker`: المحرك التنفيذي المسؤول عن:
     - فحص القفل الفردي (`fcntl.flock`) لضمان عدم تشغيل نسختين من الخادم.
     - فتح وإدارة الاتصال التسلسلي مع شريحة CH340 بمعدل 9600 باود.
     - تنفيذ فحص الإقلاع الذاتي (**Boot Self-Test**): تفعيل ثم إطفاء القنوات الأربع بالتتابع للتأكد من سلامة اللوحة.
     - تطبيق خوارزمية **بوابة OR (OR-Gate Reduction)**:
       إذا كانت الكاميرا 1 والكاميرا 2 مربوطتين بالقناة رقم 2؛ فإن القناة 2 تظل مغلقة (ON) طالما أن أيٍّ من الكاميرتين في حالة إنذار، ولا تُفتح (OFF) إلا إذا زال الخطر من الكاميرتين معاً.
     - إرسال أوامر التحكم التسلسلية للوحة RM04U:
       - `N1`, `N2`, `N3`, `N4` لتشغيل القنوات (ON).
       - `F1`, `F2`, `F3`, `F4` لإطفاء القنوات (OFF).

#### 9. `ai_gui_system.py` (الواجهة الرئيسية ومنظومة الذكاء الاصطناعي الصناعية)
- **الوظيفة:** أضخم ملف في المشروع، يمثل نقطة انطلاق العملية الأولى ويحتوي على كافة عناصر واجهة المشغل ولوحات الإعدادات مع تحصين صناعي كامل.
- **المكونات والترقيات الأساسية داخل الملف:**
  1. **الربط الفيزيائي لمنافذ USB (`get_physical_usb_path`):**
     قراءة مسار الكاميرا من النواة (`/sys/class/video4linux/videoX/device`) والربط بالمنفذ الفيزيائي في اللوحة الأم (مثل `1-1.2`) لمنع تبديل الكاميرات عند اهتزاز أسلاك الرافعة الشوكية.
  2. `CameraWorkerThread`: خيط معالجة مستقل (`QThread`) لكل كاميرا من الكاميرات الأربع:
     - يلتقط إطارات الفيديو ويدعم إعادة الاتصال الحصري بنفس المنفذ الفيزيائي.
     - تخطي إطارات متكيف ومعالجة فريمات YOLO بدون تأخير.
     - فحص تداخل الأشخاص مع الـ ROI المحسوب نسبياً والمطابق بدقة لأبعاد الإطار.
  3. `CameraCardWidget`: بطاقة العرض الخاصة بكل محطة:
     - **قفل شبكة العرض (2x2 Invariant Geometry):** سياسة حجم `QSizePolicy.Expanding` للبطاقة و `QSizePolicy.Ignored` لنافذة الفيديو لمنع أي تمدد عند انقطاع الإشارة.
     - **لوحة التنبيه الثابتة (`update_offline_banner`):** رسم التنبيه بدقة قياسية `640x480` دون استطلاع أبعاد الودجت الحالية، مما يمنع حلقات التكبير العشوائي (Stretch Loop).
     - **قائمة اختيار الريلاي لكل محطة (`⚡ Relay:`):** تتيح اختيار القناة (Relay 1 - Relay 4) مع مزامنة فورية مع الإعدادات وملف `config.json`.
     - **زر فحص الريلاي المباشر (`⚡ TEST R1`-`R4`):** زر تفاعلي يمكن المشغل من تجربة وتشغيل الريلاي فيزيائياً من شاشة المراقبة مباشرة.
  4. `ForkliftSafetyGUI`: النافذة الرئيسية للنظام:
     - **واجهة متكيفة عالمياً (Universal Responsive UI):** معامل تكبير ديناميكي `_ui_scale` يتدرج من `0.45` للشاشات المدمجة الصغيرة (800x480 و 1024x600) وحتى `1.50` لشاشات 4K، مما يضمن ظهور جميع العناصر بدون اقتصاص.
     - **شبكة العرض المتوازنة (2x2 Grid Layout):** توزيع صارم بمعدل 50% عرض و 50% ارتفاع لكل محطة باستخدام `setRowStretch` و `setColumnStretch`.
     - **وضع Kiosk الصناعي وقفل Alt+F4:** منع الإغلاق العرضي وتأمين الحماية بمستويات الصلاحيات الأربعة (RBAC).

---

### 5.4 الحزمة المعيارية المنظمة (`forklift_ai_safety`)

تمت إعادة هيكلة المشروع في حزمة برمجية قياسية تحت اسم `forklift_ai_safety/` لتقسيم الكود إلى طبقات نظيفة قابلة للصيانة والتطوير المستقبلي:

#### 1. مجلد النواة (`forklift_ai_safety/core/`):
- `camera.py`: تعريف فئة الكاميرا واستخراج خصائص الفيديو.
- `camera_thread.py`: إدارة خيط قراءة الكاميرا ومعالجة أحداث التدفق.
- `detector.py`: فئة كاشف المشاة المعزولة عن أي عناصر رسومية.
- `roi_manager.py`: إدارة حسابات المناطق وحفظها وقراءتها من ملفات JSON.
- `alarm_manager.py`: إدارة شروط إطلاق الإنذار والأصوات التنبيهية.
- `security.py`: منطق التحقق الأمني وإدارة الجلسات والأدوار.

#### 2. مجلد واجهات المستخدم (`forklift_ai_safety/ui/`):
- `main_window.py`: النافذة الرئيسية المعيارية وتنسيق اللوحات.
- `camera_panel.py`: عنصر واجهة الكاميرا الفردية.
- `video_panel.py`: الحاوية المسؤولة عن تنظيم شبكة الكاميرات.
- `sidebar_panel.py`: شريط الأدوات وسجل التدقيق اللحظي.
- `status_bar.py`: الشريط السفلي لعرض حالة الاتصال وزمن التشغيل.

#### 3. نقطة الدخول (`forklift_ai_safety/main.py`):
نقطة الإقلاع الخاصة بهذه الحزمة المهيكلة التي تقوم بتهيئة تطبيق Qt وعرض النافذة الرئيسية.

---

### 5.5 ملفات الاختبارات والتحقق (Test Suites)

#### `test_or_gate_logic.py` (حزمة اختبارات بوابة OR والعتاد)
- **الوظيفة:** ملف اختبارات شامل يحتوي على 13 اختبار وحدات واختبار إجهاد متقدم للتحقق الصارم من منطق التحكم بالعتاد دون الحاجة لوجود ريلاي فيزيائي متصل أثناء الفحص.
- **الفئات والمحاكيات المضمنة:**
  - `FakeSerial`: محاكي برمجي للمنفذ التسلسلي يسجل كافة الأوامر المكتوبة للتأكد من صحتها (`N1`, `F1`, إلخ).
  - `FakeIPCServer`: محاكي لخادم المقابس للتحقق من سلامة استقبال وإرسال حزم الـ JSON.
  - `TestORGateLogic`: فئة اختبارات تغطي:
    - سيناريو اشتراك محطتين في نفس القناة وتداخلهما الزمني.
    - سيناريو تحرير محطة بينما لا تزال المحطة الأخرى في حالة خطر.
    - اختبار فحص الإقلاع الذاتي (Boot Self-Test).
    - اختبار مفتاح الملف الكهربائي البرمجي (Hardware Coil Enable/Disable).
    - اختبار الأوامر اليدوية وتحديث التعيين اللحظي.
  - `TestORGateLatencyStress`: اختبار إجهاد يحاكي **10,000 دورة تحفيز عشوائية** لقياس معدل التأخير (Latency) والتأكد من عدم وجود تسريب في الذاكرة أو تجمد في المعالجة.

#### حزم الاختبارات المتقدمة في مجلد `scratch/`:
1. **`test_roi_and_layout_stability.py` (اختبار ثبات الشبكة والـ ROI النسبي):**
   - التحقق من ثبات `sizeHint` لـ `VideoLabel` عند تعيين صور بفريمات وأحجام مختلفة.
   - التحقق من دقة التكبير والتصغير التلقائي لمربع الـ ROI النسبي عبر `resizeEvent`.
   - التحقق من ثبات لوحة الأوفلاين عند 640x480 وحفظ الإحداثيات النسبية في `config.json`.
   - التحقق من مطابقة الإحداثيات النسبية مع إطارات YOLO المباشرة.
2. **`test_multi_resolution_adaptation.py` (اختبار التكيف مع مختلف مقاسات الشاشات):**
   - فحص آلي لمحاكاة 9 مقاسات شاشات قياسية وصناعية من `800x480` و `1024x600` وحتى `4K UHD (3840x2160)`.
   - التأكد من عدم تجاوز أي نافذة أو ودجت لحدود الشاشة واكتمال قابلية القراءة لكافة عناصر التحكم.
3. **`test_card_relay_and_camera_dropdown.py` (اختبار ميزات المحطات في الواجهة):**
   - التحقق من تزامن قوائم اختيار الريلاي وزر الفحص اليدوي في بطاقة كل محطة مع الإعدادات.
   - فحص القوائم المنسدلة للكاميرات وتوليد الخيارات الفيزيائية والعقد الحقيقية والافتراضية.
4. **`test_physical_usb_binding.py` (اختبار الربط الفيزيائي لمنافذ USB):**
   - التحقق من استخراج مسارات الـ Hubs والشجرة الفيزيائية بالرجوع العكسي في `sysfs`.
   - منع تبديل وتداخل الكاميرات بين المحطات الأربع واختبار قفل التسجيل الآمن خيطياً.
5. **`test_production_architecture.py` (اختبار البنية الإنتاجية ونظام الأمان):**
   - التحقق من أداء مقبس الـ IPC السريع وتشفير وفك حزم الـ JSON.
   - التحقق من تكامل لوحة المفاتيح الافتراضية OSK مع حقول الإدخال والتحكم بالأدوار الأربعة.


---

## 6. بروتوكول التواصل بين العمليات (IPC Protocol Details)

يتم التواصل بين العمليتين عبر مقبس يونكس موحد (`/dev/shm/forklift_relay.sock`) باستخدام رسائل نصية بترميز JSON تنتهي بسطر جديد (`\n`):

### 6.1 الأوامر الصادرة من الواجهة إلى خادم العتاد (Client ➔ Server):
1. **أمر تفعيل/إيقاف إنذار محطة:**
   ```json
   {"cmd": "trigger", "station": 0, "state": 1}
   ```
2. **أمر تحديث مصفوفة ربط القنوات:**
   ```json
   {"cmd": "relay_mapping", "mapping": [2, 2, 2, 2]}
   ```
3. **أمر تحديث أزمنة التأخير والملف البرمجي:**
   ```json
   {"cmd": "config", "on_delay": 0.0, "off_delay": 10.0, "hardware_coil_enabled": 1}
   ```
4. **أمر الاختبار اليدوي لقناة ريلاي:**
   ```json
   {"cmd": "manual_test", "channel": 1, "state": 1}
   ```
5. **طلب حالة العتاد:**
   ```json
   {"cmd": "status"}
   ```

### 6.2 الرسائل الصادرة من خادم العتاد إلى الواجهة (Server ➔ Client):
1. **تحديث حالة العتاد الدورية:**
   ```json
   {"type": "status", "hw_status": "ONLINE", "states": [false, true, false, false]}
   ```
2. **تأكيد استلام الأوامر (ACK):**
   ```json
   {"type": "ack", "cmd": "trigger", "success": true}
   ```

---

## 7. التحكم بالعتاد الفيزيائي ومصفوفة الريلاي (Hardware & Relay Logic)

### 7.1 مواصفات لوحة الترحيل (RM04U USB Relay)
- **المعالج/الشريحة:** CH340 USB-to-UART Serial.
- **منافذ الخرج:** 4 قنوات ترحيل كهروميكانيكية (Normally Open / Normally Closed).
- **البروتوكول التسلسلي:**
  - معدل نقل البيانات: 9600 باود.
  - بت البيانات: 8، بدون بت تحقق (No Parity)، بت توقف 1 (8-N-1).
  - أوامر التشغيل (ASCII Commands):
    - تشغيل القنوات 1 إلى 4: `N1`, `N2`, `N3`, `N4`.
    - إطفاء القنوات 1 إلى 4: `F1`, `F2`, `F3`, `F4`.

### 7.2 جدول منطق بوابة OR لتعدد المحطات
عند ربط المحطة 0 والمحطة 1 بالقناة الفيزيائية رقم 2، تكون العلاقة المنطقية كالتالي:

| إنذار المحطة 0 | إنذار المحطة 1 | الحالة الفيزيائية للقناة 2 | الأمر المرسل للوحة |
|:---:|:---:|:---:|:---:|
| 0 (آمن) | 0 (آمن) | مطفأة (OFF) | `F2` |
| 1 (خطر) | 0 (آمن) | تعمل (ON) | `N2` |
| 0 (آمن) | 1 (خطر) | تعمل (ON) | `N2` |
| 1 (خطر) | 1 (خطر) | تعمل (ON) | `N2` |

> [!IMPORTANT]
> يضمن هذا المنطق الصناعي عدم قيام أي محطة بإطفاء السارينة طالما أن هناك محطة أخرى لا تزال ترصد وجود شخص في مسار الخطر.

---

## 8. منظومة الأمان والصلاحيات (Role-Based Access Control)

يقسم النظام صلاحيات المستخدمين إلى خمسة مستويات هرمية:

```
┌────────────────────────────────────────────────────────┐
│ 5. Administrator  (superadmin) — صلاحية كاملة وتغيير الرمز │
├────────────────────────────────────────────────────────┤
│ 4. Engineer       (admin)      — ضبط خوارزميات الذكاء   │
├────────────────────────────────────────────────────────┤
│ 3. Supervisor     (1111)       — تعديل ROI وإغلاق النظام │
├────────────────────────────────────────────────────────┤
│ 2. Technician     (tech)       — اختبار الريلاي والكاميرا│
├────────────────────────────────────────────────────────┤
│ 1. Operator       (لا كلمة سر) — مراقبة حية فقط         │
└────────────────────────────────────────────────────────┘
```

- **قفل الخروج من النظام (Kiosk Lock):** لا يستطيع أي مستخدم إغلاق البرنامج عبر `Alt+F4` أو أزرار النافذة إلا بعد الترقية لدور **Supervisor** على الأقل وإدخال الرمز السري.
- **تسجيل الخروج التلقائي:** لحماية النظام في صالات الإنتاج، يتم تفعيل مؤقت خمول لمدة 5 دقائق يعيد النظام تلقائياً إلى وضع `Operator` في حال غياب المشرف.

---

## 9. دليل التشغيل، الصيانة واستكشاف الأخطاء (Troubleshooting Guide)

### 9.1 أوامر التشغيل والتحكم اليدوي وإدارة الخدمة

1. **التحكم بخدمة النظام التلقائية (Systemd Service):**
   ```bash
   # معرفة الحالة اللحظية للخدمة والعمليات التابعة لها
   systemctl --user status forklift-ai.service

   # إعادة تشغيل المنظومة بالكامل
   systemctl --user restart forklift-ai.service

   # إيقاف المنظومة مؤقتاً لأعمال الصيانة
   systemctl --user stop forklift-ai.service

   # بدء تشغيل المنظومة
   systemctl --user start forklift-ai.service

   # متابعة السجلات الحية لجميع المكونات (Logs Stream)
   journalctl --user -u forklift-ai.service -f
   ```

2. **تشغيل النظام يدوياً من الطرفية (وضع التطوير والصيانة):**
   ```bash
   cd ~/Desktop/AI_Forklift_Safety
   # تشغيل عادي مع مهلة الـ 15 ثانية لاستقرار العتاد
   ./start_system.sh

   # تشغيل فوري سريع دون انتظار الـ 15 ثانية
   ./start_system.sh --no-delay
   ```

3. **مراقبة سجل تشغيل العتاد لحظياً:**
   ```bash
   tail -f ~/Desktop/AI_Forklift_Safety/hardware.log
   ```

4. **فحص العمليات المشغلة في الذاكرة:**
   ```bash
   ps aux | grep -E "ai_gui_system|hardware_worker"
   ```

### 9.2 المشاكل الشائعة وطرق علاجها

#### 1. خادم العتاد يظهر حالة OFFLINE على الواجهة
- **السبب:** عدم توصيل كابل USB للريلاي أو عدم وجود صلاحيات المنفذ.
- **الحل:**
  - تأكد من ظهور الجهاز عبر كتابة `lsusb`.
  - تحقق من صلاحيات المستخدم: `sudo usermod -a -G dialout $USER`.
  - تأكد من وجود ملف المقبس: `ls -l /dev/shm/forklift_relay.sock`.

#### 2. انقطاع بث إحدى الكاميرات (ظهور إشارة تحذير الكاميرا)
- **السبب:** ارتخاء كابل الكاميرا أو انقطاع التغذية اللحظي.
- **الحل:**
  - يقوم النظام تلقائياً وبفضل ميزة **Physical USB Binding** بمحاولة إعادة فتح الكاميرا المرتبطة بنفس المنفذ الفيزيائي كل 3 ثوانٍ دون تبديل المحطات.
  - للتحقق من الكاميرات والمنافذ المتصلة في النظام:
    ```bash
    v4l2-ctl --list-devices
    ```

#### 3. بطء الاستجابة أو انخفاض معدل الإطارات (Low FPS)
- **السبب:** ارتفاع استهلاك المعالج بسبب معالجة 4 كاميرات في آنٍ واحد.
- **الحل:**
  - النظام مجهز بمؤقت 1 ثانية لمعالجة YOLO وخوارزمية معالجة أحدث فريم متاح فقط دون تراكم في الطوابير (Zero-Queue Latency).
  - تم تحسين أبعاد إدخال المعالجة قسرياً إلى `imgsz=320` في `detector.py`.

---

## 10. الترقيات والتحسينات الإنتاجية الحديثة (Production Upgrades & Industrial Resilience)

تم تزويد النظام بأحدث المعايير الصناعية لضمان استقرار العمل بنسبة 99.99% على الرافعات الشوكية في بيئات التشغيل القاسية:

### 10.1 الربط الفيزيائي لمنافذ USB ومنع تبديل الكاميرات عند الاهتزاز (Physical USB Port Binding)
- **المشكلة:** تؤدي اهتزازات الرافعة الشوكية إلى فصل لحظي لكوابل USB، وعند إعادة الاتصال يُسند لينكس أرقام عقد جديدة عشوائياً (مثل تحول الكاميرا الخلفية من `/dev/video0` إلى `/dev/video8`)، مما كان يسبب خطراً كارثياً بتبديل شاشات المراقبة.
- **الحل المطبق:**
  - دمج دالة `get_physical_usb_path()` التي تقرأ مسار الجهاز الحقيقي من شجرة النواة (`/sys/class/video4linux/videoX/device`) وتربطه برقم المنفذ الفيزيائي على اللوحة الأم وموزعات الـ USB (مثل `1-1.2` و `1-1.4`).
  - تأمين سجل الكاميرات بقفل خيطي عام `_registry_lock` يمنع أي محطة من الاستيلاء على كاميرا تخص محطة أخرى نهائياً.

### 10.2 التحكم والربط اللحظي للريلاي لكل محطة وزر الفحص المباشر (Station Relay Binding & Live Testing)
- تزويد بطاقة كل محطة (`CameraCardWidget`) بقائمة منسدلة `⚡ Relay:` تتيح للمشغل أو المشرف اختيار قناة الريلاي المرتبطة بتلك المحطة (Relay 1 إلى Relay 4) مع حفظ ومزامنة ثنائية وفورية في `config.json` وخادم العتاد.
- إضافة زر فحص مخصص وتفاعلي لكل محطة (`⚡ TEST R1`-`R4`) يتيح تفعيل وإلغاء الريلاي فيزيائياً من شاشة المراقبة مباشرة مع وميض تحذيري باللون الأحمر دون الحاجة لمغادرة الشاشة الحية.

### 10.3 إدارة الخدمة والاسترداد الذاتي عبر Systemd وحل تضارب التشغيل (Systemd Self-Healing Service)
- إنشاء خدمة نظام متكاملة للمستخدم: `~/.config/systemd/user/forklift-ai.service` بسياسة إعادة تشغيل إجبارية `Restart=always` و `RestartSec=3` تضمن عودة النظام للعمل خلال 3 ثوانٍ فقط من أي توقف أو كراش مفاجئ.
- تفعيل خاصية الـ Lingering عبر `loginctl enable-linger forklift` لتظل الخدمة تعمل مع إقلاع الجهاز دون اشتراط وجود جلسة طرفية نشطة.
- توحيد مسارات الإقلاع التلقائي في النظام (`~/.config/autostart/AI_Forklift_Safety.desktop` و `/home/forklift/Desktop/run_system.sh`) لتوجه أمرها إلى Systemd عبر `systemctl --user restart forklift-ai.service`، مما قضى على تضارب البرامج ومنع تشغيل نسختين في آنٍ واحد.

### 10.4 مهلة استقرار العتاد ومنافذ الـ USB عند الإقلاع (15s Hardware Stabilization Delay)
- إضافة مؤقت عد تنازلي حي مدته 15 ثانية في بداية تشغيل `start_system.sh` لإعطاء النواة ولينكس والموزعات مهلة كافية لاكتشاف كافة الكاميرات وقنوات V4L2 ومتحكم السيريال CH340 واستقرار التغذية الكهربائية قبل محاولة فتحها.
- إتاحة راية التخطي السريع للمطورين (`./start_system.sh --no-delay` أو `SKIP_DELAY=1`) لتشغيل النظام فورياً أثناء التجارب البرمجية.

### 10.5 حماية شبكة العرض من التمدد والتكبير عند انقطاع الكاميرات (Layout Stretch Prevention)
- **المشكلة:** عند انقطاع أي كاميرا، كانت دالة التنبيه تعيد إنشاء صورة بأبعاد متغيرة ديناميكياً مما كان يدفع مدير الشبكة `QGridLayout` لتغيير أبعاد الأعمدة والتسبب في تمدد أو تكبير الكروت الأخرى (Stretch).
- **الحل المطبق:**
  - قفل شبكة الـ 2x2 بمعاملات تمدد متساوية 50% عرض و 50% ارتفاع لكل محطة (`setRowStretch(0, 1)`, `setRowStretch(1, 1)`, `setColumnStretch(0, 1)`, `setColumnStretch(1, 1)`).
  - تثبيت سياسة حجم ودجت الفيديو `self.video_label.setSizePolicy(QSizePolicy.Ignored, QSizePolicy.Ignored)` لعزل تخطيط الشبكة تماماً عن أبعاد الإطارات أو لوحات التنبيه.
  - تثبيت دالتي `sizeHint()` و `minimumSizeHint()` في `VideoLabel` لترجع حجماً ثابتاً لا يتغير بتغير الصور (`320x240`).
  - تثبيت لوحة التنبيه عند الانقطاع `update_offline_banner()` على دقة قياسية معيارية ثابتة `640x480` بنسبة 4:3.

### 10.6 نظام الإحداثيات النسبية المعيارية لمربع الـ ROI (Normalized Coordinates 0.0 - 1.0)
- تخزين وحساب إحداثيات مربع الـ ROI كنسب مئوية دقيقة (`norm_x`, `norm_y`, `norm_w`, `norm_h`) بين `0.0` و `1.0` بدلاً من البكسلات المطلقة.
- ربط حدث `resizeEvent()` في `VideoLabel` لإعادة رسم المربع بنسبه الحقيقية تلقائياً عند أي تغيير في حجم الشاشة أو عند فصل الكاميرا وإعادة توصيلها، مما يمنع انزياح الصندوق نهائياً.
- مطابقة فحص حدود الأشخاص ونموذج YOLO مباشرة مع فريمات الكاميرا عبر النسب المعيارية دون الاعتماد على أبعاد شاشة العرض.

### 10.7 التكيف الديناميكي مع أي نوع وأي مقاس شاشة (Universal Responsive UI: 800x480 to 4K)
- برمجة معامل تكبير مرن `_ui_scale` يتدرج تلقائياً بين `0.45` للشاشات المدمجة الصغيرة و `1.50` لشاشات الـ 4K، مما يتيح للواجهة أن تنكمش وتتمدد بسلاسة كاملة.
- تصحيح `setMinimumSize` لتكون مرنة `min(_screen_w, 640), min(_screen_h, 400)` وتمنع خروج النوافذ عن الشاشة في الشاشات الصناعية الصغيرة (مثل 1024x600 و 800x480 للرافعات).
- التحقق المخبري الناجح عبر محاكاة 9 مقاسات شاشات مختلفة:
  `800x480`, `1024x600`, `1024x768`, `1280x720`, `1280x800`, `1366x768`, `1920x1080`, `2560x1440`, `3840x2160`.

---

## 11. سجل معالجة العيوب والتحصين البرمجي المكتمل (Defect Remediation & Hardening Log)

خلال المراجعة الهندسية الشاملة للمنظومة (سبتمبر 2026)، تم رصد ومعالجة 8 عيوب برمجية بدقة متناهية لضمان ثبات النظام الميداني:

| رقم الخطأ | المشكلة البرمجية | الإجراء المتخذ في الكود | النتيجة والتحقق |
|:---:|---|---|---|
| **1** | احتجاز مؤقت YOLO والوميض داخل `on_physical_path_discovered` مما عطل الاستدلال عند الإقلاع | تم نقل تشغيل `yolo_timer` و `blink_timer` إلى نهاية `start_camera()` في `ai_gui_system.py` | ✅ الاستدلال يعمل فورياً ومستقلاً عن آلية فحص المسارات |
| **2** | انهيار التطبيق عند حفظ مسار YOLO بسبب تضارب `model_name` و `model_path` | دعم كلا الوسيطين دفاعياً في `detector.py` وتمرير `model_name` في `ai_gui_system.py` | ✅ تبديل الأوزان وحفظها بسلاسة دون أي كراش |
| **3** | خطر `AttributeError` بسبب عدم تهيئة `manual_overrides` بالدالة البانية | تهيئة `self.manual_overrides = [False, False, False, False]` في `hardware_manager.py:65` | ✅ القضاء التام على أي حالة سباق برمجية |
| **4** | عدم مزامنة المنفذ التسلسلي وسرعة الباود مع خادم العتاد أثناء التشغيل | توجيه التعديلات وتنبيه المستخدم لإعادة تشغيل خدمة Systemd لتطبيق المنفذ على الخادم | ✅ استقرار وسائط الاتصال التسلسلي |
| **5** | إغراق سجلات `journalctl` واستنزاف المعالج بتحذيرات OpenCV C++ | إضافة `cv2.setLogLevel(0)` و `os.environ["OPENCV_LOG_LEVEL"] = "SILENT"` | ✅ سجلات نظيفة تماماً وخفض استهلاك الذاكرة والمعالج |
| **6** | انقطاع بث الكاميرا يُظهر تحذير اختراق مسار بدلاً من تنبيه عطل كاميرا | فصل خاصية الخطأ عن الإنذار وعرض إشعار تشخيصي خاص بحالة الكاميرا | ✅ وضوح كامل لسائقي الرافعات وفرق الصيانة |
| **7** | تسجيل زمن إطفاء جامد (3s) في سجل العمليات بغض النظر عن إعدادات `config.json` | قراءة قيمة `alarm_auto_reset` ديناميكياً وتنسيقها في سجل التدقيق | ✅ مطابقة كاملة لسجلات التدقيق الصناعي |
| **8** | تجميد واجهة المستخدم لعدة ثوانٍ عند إيقاف الكاميرات في `pause_all_card_timers` | تقليص مهلة انتظار إنهاء الخيوط وضمان تحرير الموارد دون حجب خيط الواجهة | ✅ واجهة سلسة وسريعة الاستجابة على شاشات اللمس |

### نتائج اختبارات التحقق الشاملة (100% PASS):
- **اختبار بوابة OR والأزمنة الميكروثانية:** 13/13 اجتياز في 0.214 ثانية (متوسط زمن الاستجابة 0.0168 ملي ثانية).
- **اختبار الربط الفيزيائي لمنافذ USB:** 9/9 اجتياز في 0.010 ثانية.
- **اختبار هندسة الـ ROI النسبية:** 5/5 اجتياز وثبات مطلق للأبعاد.
- **اختبار التكيف مع مختلف دقات الشاشات:** 9/9 اجتياز عبر دقات من 800x480 إلى 4K.

---
*تم تحديث هذا التوثيق ليعكس البنية الإنتاجية المحدثة لنظام Limitless Future AI Forklift Safety System.*

